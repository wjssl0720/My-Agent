# Production Context Management

上下文结构：

```text
ContextPack
ContextBudget
ContextWindow
ContextSummary
ProductionContextCompactor
BusinessContextExtractor
FileContextLoader
RagContextLoader
ToolObservationContext
```

目标不是把所有信息都塞进 prompt，而是按预算选择。
